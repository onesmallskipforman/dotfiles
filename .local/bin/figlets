#!/bin/sh

msg=${1:-"Test"}
# for f in {/usr/local/share/figlet/fonts,$HOME/.local/src/figlet-fonts}/*.flf; do
#   echo "$f"
#   figlet -w 100 -f "$f" "$msg"
# done

ls {/usr/local/share/figlet/fonts,$HOME/.local/src/figlet-fonts}/*.flf \
  | fzf --with-nth -1 -d / --preview "figlet -w 100 -f {} $msg"

#!/bin/zsh
# /usr/local/share/figlet/fonts/**/*.flf | fzf --with-nth -1 -d / --preview 'figlet -f {} abc'
