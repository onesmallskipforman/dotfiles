#!/bin/sh

sudo systemctl restart docker
docker container prune -f
docker image prune -f
docker images -a -q | xargs -r docker rmi -f
docker image prune -af
docker system prune -af
docker container ls && docker images
